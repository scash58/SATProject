@model SATProject.DATA.EF.StudentStatus

@{
    ViewBag.Title = $"Delete {Model.SSName} ?";
}

<div class="m-2">
    <div class="text-center">
        <h2>@ViewBag.Title</h2>
        <h4>Student Status</h4>
    </div>


    <div>
        <hr />
        <div class="text-center">
            <div>
                <b>@Html.DisplayNameFor(model => model.SSName)</b>
            </div>

            <div>
                @Html.DisplayFor(model => model.SSName)
            </div>

            <div>
                <b>@Html.DisplayNameFor(model => model.SSDescription)</b>
            </div>

            <div>
                @Html.DisplayFor(model => model.SSDescription)
            </div>

        </div>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-group row text-center">
                <div class="p-2">
                    <input type="submit" value="Delete" class="btn btn-danger" />
                </div>
                <div>
                    @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-primary" })
                </div>
            </div>
        }
    </div>
</div>